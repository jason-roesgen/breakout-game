import { Game } from "./game.js";

// class vector
/// public property x
/// public property y
/// constructor parameters x,y: numbers

export class Vector
{
  public x: number;
  public y: number;

  constructor( x: number, y: number )
  {
    this.x = x;
    this.y = y;
  }
}
